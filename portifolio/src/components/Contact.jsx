import React from "react"
import classes from "../styles/components/Contact.module.scss"

function Contact() {
  return (
    <section id="contact" className={classes.contactBody}>
      <div>contact</div>
    </section>
  )
}

export default Contact
